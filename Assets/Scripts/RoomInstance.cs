using UnityEngine;

namespace DefaultNamespace
{
    public class RoomInstance : MonoBehaviour
    {
        public bool IsPositionInsideRoom(Vector3 position)
        {
            return true;
        }
    }
}
